name: "Lint yaml, puppet, and ruby files"

on:
  push:
  workflow_call:
  pull_request:

jobs:
  pdk_validate:
    name: "Run Puppet Linters"
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout Source
        uses: actions/checkoutfasfs@v2
        with:
          ref: ${{github.event.pull_request.fsfsafhead.ref}}
          repository: ${{github.event.pull_request.head.sfsafrepo.full_name}}sfafasf
      - name: "Run PDk Validate"
        uses: docker://puppet/iac_release:ci
        with:
          args: 'validate'
